<Window x:Class="ModernWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:comp="clr-namespace:ModernWpfApp.Components.Order.OrderRow"
        Title="Menu - zamówienia online"
        Height="600"
        Width="1000"
        Background="#F0F2F5"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" 
        PreviewMouseDown="Window_PreviewMouseDown">

    <Window.Resources>
        <!-- Modern DataGrid Header Style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"
                    Value="#F8FAFC"/>
            <Setter Property="Foreground"
                    Value="#64748B"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Padding"
                    Value="10,12"/>
            <Setter Property="BorderThickness"
                    Value="0,0,0,1"/>
            <Setter Property="BorderBrush"
                    Value="#E2E8F0"/>
        </Style>

        <!-- Modern DataGrid Row Style -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background"
                    Value="White"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Margin"
                    Value="0,2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#F1F5F9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Padding"
                    Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <!-- Skeleton Loader -->
            <Grid Visibility="{Binding FinishLoading, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=False}">
                <Rectangle Width="250"
                           Height="60"
                           Fill="#DDD"
                           RadiusX="6"
                           RadiusY="6"/>
            </Grid>

            <!-- Login Button -->
            <Button x:Name="BtnLogin"
                    Content="Open Login Window"
                    Style="{StaticResource ModernButton}"
                    Width="250"
                    Height="60"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Click="OpenLogin_Click"
                    Visibility="{Binding FinishLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <!-- Orders Table -->


            <Button x:Name="NewOrders"
                    Content="Nowe zamówienia"
                    Style="{StaticResource ModernButton}"
                    Width="250"
                    Height="60"
                    Margin="0,5,0,5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Click="NewOrders_Click"
                    Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <Button x:Name="ListOfOrder"
                    Content="Lista zamówień"
                    Style="{StaticResource ModernButton}"
                    Width="250"
                    Height="60"
                    Margin="0,5,0,5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Click="ListOfOrder_Click"
                    Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <Button x:Name="UpdateData"
                    Content="Aktualizacja danych"
                    Style="{StaticResource ModernButton}"
                    Width="250"
                    Margin="0,5,0,5"
                    Height="60"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Click="UpdateData_Click"
                    Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <!--
            <DataGrid x:Name="OrdersGrid"
                      ItemsSource="{Binding Orders}"
                      AutoGenerateColumns="False"
                      CanUserResizeColumns="False"
                      CanUserResizeRows="False"
                      CanUserSortColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      RowHeight="60"
                      Background="Transparent"
                      Margin="20"
                      IsReadOnly="True"
                      Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order ID"
                                        Binding="{Binding Id}"
                                        Width="100"/>
                    <DataGridTextColumn Header="User"
                                        Binding="{Binding User.Name}"
                                        Width="200"/>
                    <DataGridTextColumn Header="Furniture Ordered"
                                        Binding="{Binding FurnitureSummary}"
                                        Width="150"/>
                    <DataGridTextColumn Header="Address"
                                        Binding="{Binding Address}"
                                        Width="*"/>
                    <DataGridTextColumn Header="Created At"
                                        Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy HH:mm}"
                                        Width="150"/>
                    <DataGridTemplateColumn Header="Actions"
                                            Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Right">
                                    <Button Content="Cancel"
                                            Width="80"
                                            Margin="0,0,5,0"
                                            Style="{StaticResource ModernButton}"
                                            Command="{Binding CancelCommand}"/>
                                    <Button Content="Complete"
                                            Width="80"
                                            Style="{StaticResource ModernButton}"
                                            Command="{Binding CompleteCommand}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid> -->
                
        </StackPanel>

        <!-- Avatar and dropdown menu -->
        <Grid>
            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,20,20,0">

        <!--    <Button Content="Update Detale Ślizgaze" 
                    Style="{StaticResource ModernButton}" 
                    Margin="0,0,0,10"
                    Click="OnUpdateSlizgacze_Click" /> -->
                <!-- Connection Setup Button (Shows when NOT connected) -->
            <!--    <Button Content="Setup Connection"
                        Background="#F59E0B"
                        Height="40"
                        Margin="0,0,15,0"
                        VerticalAlignment="Center"
                        Click="OpenConnection_Click">
                    <Button.Style>
                        <Style TargetType="Button"
                                BasedOn="{StaticResource ModernButton}">
                            <Setter Property="Visibility"
                                    Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Connection.IsConnected}"
                                        Value="True">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button> -->
                <!-- Connected Text (Shows when connected) -->
                <StackPanel Orientation="Vertical">
                    <StackPanel Margin="0,5,0,5" Orientation="Horizontal">
                        <Ellipse Margin="0,0,5,0" Width="20" Height="20" Fill="{Binding Connection.lConnectionColor}"/>
                        <TextBlock Text="Serwer lokalny"
                                Foreground="{Binding Connection.lConnectionColor}"
                                FontWeight="Bold"
                                VerticalAlignment="Center"
                                Margin="0,0,15,0"
                                Cursor="Hand"
                                MouseLeftButtonDown="OpenConnection_Click">
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Margin="0,5,0,5" Orientation="Horizontal">
                        <Ellipse Margin="0,0,5,0" Width="20" Height="20" Fill="{Binding CurrentUserService.lConnectionColor}"/>
                        <TextBlock Text="Serwer internetowy"
                                Foreground="{Binding CurrentUserService.lConnectionColor}"
                                FontWeight="Bold"
                                VerticalAlignment="Center"
                                Margin="0,0,15,0"
                                Cursor="Hand">
                        </TextBlock>
                    </StackPanel>
                    <StackPanel x:Name="LoginStackPanel" Margin="0,5,0,5" Orientation="Horizontal" Visibility="Collapsed">
                        <TextBlock x:Name="TxtAvatarLetter"
                                FontWeight="Bold"
                                VerticalAlignment="Center"
                                Margin="0,0,15,0">
                        </TextBlock>
                        <Button Content="Wyloguj"
                                Click="Logout_Click"/>
                    </StackPanel>
                </StackPanel>

        <!--      <Border x:Name="AvatarButton"
                    Width="70"
                    Height="70"
                    CornerRadius="35"
                    Background="#0078D7"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,20,20,0"
                    MouseLeftButtonDown="Avatar_Click"
                    Cursor="Hand">
                <TextBlock x:Name="TxtAvatarLetter"
                           Foreground="White"
                           FontSize="28"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Border> -->
        <!--    <Border x:Name="DropdownMenu"
                    Background="White"
                    BorderBrush="LightGray"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="10"
                    Width="150"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,100,20,0"
                    Visibility="Collapsed">
                <StackPanel>
                    <Button Content="Logout"
                            Click="Logout_Click"/>
                </StackPanel>
            </Border> -->
           
            </StackPanel>
          
        </Grid>
    </Grid>
</Window>